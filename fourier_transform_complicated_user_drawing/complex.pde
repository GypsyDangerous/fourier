class complex
{
  
  float real, imaginary, freq, amp, phase;
  
  
  complex(float a, float b, float freq, float amp, float phase)
  {
    real = a;
    imaginary = b;
    this.freq = freq;
    this.amp = amp;
    this.phase = phase;
  }
}
